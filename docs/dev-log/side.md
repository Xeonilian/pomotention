# 支线 · 喝水 Planner（未发布）

> 主线仍是收费关，见 [`current.md`](./current.md)。本文件 = 插入推进的真相，可与主线并存。

| 项 | 内容 |
|---|---|
| **状态** | 周/月/年喝水图层雏形已通；日 Task 喝水表单开了 **双变体**，观感未过关 |
| **更新** | 2026-09-09 |
| **出口** | 完成下方 polish 再考虑 CHANGELOG / 发版；未完成可长期停在 side |

---

## 已对齐（勿重绕）

- 日 = 写；周/月/年 = 图层读；Task 始终日明细  
- **周**：图层可叠；任一层开 → 藏普通时间块；喝水 = 平缓水位 + 水滴横向散落 + 底栏总量  
- **月/年**：互斥单层；月 = 水位 + ×N/ml；年 = 有记录点着色（字色区分达标）  
- 生活系统 tag：不参与年点取色、不进 Tag Manager  
- **表单双变体（勿互相覆盖）**  
  - **A compact**：窄槽（周/月等）— `LifeRecordForm` drink 分支 + `DrinkSipTag`；水滴即 +1、text 图标、× 关闭  
  - **B day panel**：日视图 — `DrinkDayPanel.vue`；左圆水位 + 右三线表；手机上图下表；杯量脚注不进表滚  

---

## 下一刀（polish · 发布前）

1. **按钮联动** — 图层态与顶栏四键填充/提示一致；吃/厕/睡层仍可占位或暂不接 UI  
2. **手机布局** — 周列窄、水滴散落与触控（表单见下）  
3. **Task 表单观感** — **正在抠 B**；功能可用，顺眼需按点精修（对齐 / gap / 线色 / 圆与表比例），不能整片「再调一版」糊弄  

### B 日面板 · 明天从哪继续

| 点 | 现状 | 备注 |
|---|---|---|
| 壳 | 日视图走 B、非日走 A；任务区高度吃满；宽区居中 | 保留 |
| 圆 | 水位复用月格 SVG；水滴+×N 已改成同一 inline 按钮 | **观感未过**：逐项对基线/光学间距 |
| 表 | 三线表 + 表内纵滚 + 杯量脚注固定；行内量可改 | 线色已淡；列宽/字重/空态仍糙 |
| 输入 | 目标 / 量 / 杯量去白底边框 | 与两侧数字仍易「看起来不在一条线」 |

**协作约定（明天）：** 一次只精修 1～2 个点（例如「圆上水滴×N」或「表三线」），过/不过，少整页拧。

---

## Bug / 债（记下）

| 项 | 说明 | 状态 |
|---|---|---|
| 月皮肤邻月格 `×0 0ml` | 上/下月垫格不应渲染喝水文案（与日历空格一致）；SVG 已按 `isCurrentMonth` 跳过 | **已修**（2026-09-08） |

---

## 停在哪（支线）

2026-09-09：B 日面板雏形落地（`DrinkDayPanel`），A compact 保留。功能通，观感不过关；接受「按点精修」。今天收工，明天从 B 某一局部开刀。
