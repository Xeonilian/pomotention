---
title: 附录：术语对照表
description: Activity、活动清单、Todo、Task 等中英文与层级关系的集中说明，便于对照文档与界面。
---

# 附录：术语对照

本页把**数据模型**、**界面分区**与**同步存储**里常见的英文标识放在一起对照，避免把 `Activity`、`ActivitySheet`、`Todo`、`Task` 混为一谈。细节仍以各模块文档为准。

---

## 核心对照（Activity 族）

| 中文（文档常用）    | 英文 / 代码标识                                    | 指什么                                                                                                                                      | 易混点                                                                            |
| ------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| 活动（条目）        | `Activity`                                         | 出现在活动清单中的一条记录；类型包括待办、预约、无所事事、内外打扰等。                                                                      | 不是「任务追踪」里的 Task；也不是整页应用。                                       |
| 活动清单 / 右侧面板 | `ActivitySheet`（组件）、`activitySheet`（存储键） | **界面**：首页右侧管理活动的区域，由 `ActivitySheet` 组件实现。<br>**数据**：本地/同步里常以键名 `activitySheet` 保存活动数组。             | 「Sheet」在代码里是组件名；用户文档里多称「活动清单」或「活动清单视图」。         |
| 待办（待办活动）    | `Todo`                                             | `Activity` 的一种子类型：用番茄块拆分的可执行项（🍅/🍒/🍇）。                                                                               | 口语里的「待办任务」多指 `Todo`，但严格说仍是 **Activity**，不是下面的 **Task**。 |
| 任务（追踪任务）    | `Task`                                             | **任务追踪**里当前选中的执行记录：描述、能量/愉悦/打扰等时间轴都挂在这里；通常由某个 `Activity`（含 `Todo`、`Schedule` 等）进入追踪后对应。 | 与 GTD 里泛指的「任务」不同：此处特指追踪视图中的那条**执行记录**。               |

一句话串起来：**清单里的一条是 `Activity`；其中一类待办是 `Todo`；你点「追踪」后在追踪区写的是 `Task`，往往绑定到某个 `Activity`。**

---

## 与「视图」相关的说法

| 说法         | 含义                                                                                                   |
| ------------ | ------------------------------------------------------------------------------------------------------ |
| 活动清单视图 | 强调「看活动清单」这一视图状态；与任务计划、任务追踪等视图并列。                                       |
| 任务计划视图 | 按日/周/月查看与操作已排入计划的活动（今日待办、今日预约等）。见 [任务规划](../reference/planner.md)。 |
| 任务追踪视图 | 记录执行过程、能量、打扰等的区域。见 [任务追踪](../reference/task.md)。                                |

---

## 其他活动类型（同为 `Activity`）

| 中文                | 英文                                                                               |
| ------------------- | ---------------------------------------------------------------------------------- |
| 预约                | `Schedule`                                                                         |
| 无所事事            | `Untaetigkeit`（文档用语）；数据上多为带 `isUntaetigkeit: true` 的 `Schedule` 分支 |
| 内部打扰 / 外部打扰 | `Internal Interruption` / `External Interruption`                                  |

完整图标与定义见 [活动清单](../reference/activity.md)。

---

## 延伸阅读

- [活动清单](../reference/activity.md)（`Activity` / `Todo` / `Schedule` 等定义）
- [任务追踪](../reference/task.md)（`Task` 与各记录类型）
- [软件界面](../reference/interface.md)（各面板分工与联动）
- [附录：按钮一览](./buttons.md)（工具栏图标与英文标签速查）
